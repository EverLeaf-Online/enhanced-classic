#pragma once

namespace EverLeafWebLinks {
    // Redirect the legacy MapleStory login-screen web buttons to EverLeaf.
    // Call once during client startup after the process has unpacked.
    bool Install();
}
