#pragma once

namespace EverLeafLoginLayout {
    // Reposition the native v83 login controls to the tested EverLeaf HD login panel.
    // Call once after the client image has unpacked and before the login screen is used.
    void Apply();
}
