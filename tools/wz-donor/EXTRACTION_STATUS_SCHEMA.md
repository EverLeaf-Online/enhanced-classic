# Extraction status

`EXTRACTION_STATUS.json` records each staged v95 WZ family independently with `success`, exporter `exitCode`, and emitted XML count, plus aggregate family/XML totals.

The extraction workflow does not compare against v83 and does not authorize imports.
