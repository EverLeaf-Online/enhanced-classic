function enter(pi) {
    // Map.wz references this script with a lowercase 't'. Keep the exact
    // filename on Linux so the portal does not fail due to case sensitivity.
    pi.openNpc(1052125);
    return true;
}
