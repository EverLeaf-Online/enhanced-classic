<%- include("partials/header",{title:"Downloads"}) %>
<main class="lightPage"><section class="lightTitle"><div class="wrap pageHeroGrid"><div><div class="pageEyebrow">Get EverLeaf</div><h1>Download & Play</h1><p>The launcher handles installation, verification, repair, updates, and launching the game.</p></div><div class="pageHeroAside"><strong>Recommended setup</strong><span>Use a fresh folder and always start EverLeaf through the launcher so updates stay synchronized.</span></div></div></section><div class="wrap contentGrid"><div class="contentStack">
<section class="featurePanel"><div class="pageEyebrow">Official Launcher</div><div style="display:flex;justify-content:space-between;gap:18px;align-items:center;flex-wrap:wrap"><div style="display:flex;gap:16px;align-items:center"><div class="downloadIcon">🚀</div><div><h2 style="margin:0 0 6px">EverLeaf Launcher</h2><p class="muted" style="margin:0;max-width:640px">Securely installs and verifies all 36 required files, repairs damaged files, applies updates, and keeps the client ready for play.</p></div></div><a class="btn primary" href="<%=brand.launcherUrl%>">DOWNLOAD LAUNCHER</a></div></section>
<% const groups=[["launcher","Additional Launcher Builds","🚀"],["patch","Patches","🩹"],["tool","Tools","🛠"]]; %><% groups.forEach(([kind,label,icon])=>{ const items=rows.filter(r=>r.kind===kind); if(!items.length) return; %><h3 class="downloadGroupTitle"><%=label%></h3><% items.forEach(r=>{ %><div class="downloadCard"><div style="display:flex;gap:14px;align-items:center"><div class="downloadIcon"><%=icon%></div><div><strong><%=r.name%></strong><div class="muted"><%=r.description%></div><small><%=r.version?('v'+r.version):''%></small></div></div><a class="btn primary" href="<%=r.url%>">DOWNLOAD</a></div><% }) %><% }) %>
</div><aside class="contentStack"><section class="paperCard"><div class="pageEyebrow">Installation</div><h3>Four steps to Maple World</h3><ol style="padding-left:20px;line-height:1.9"><li>Create an empty folder such as <strong>Documents\EverLeaf</strong>.</li><li>Extract the launcher ZIP there.</li><li>Run <strong>EverLeafLauncher.exe</strong>.</li><li>Choose <strong>Install EverLeaf</strong>, then Play. The launcher completes verification before starting <strong>EverLeaf.exe</strong>.</li></ol></section><section class="paperCard"><h3>Having trouble?</h3><p class="muted">Use <strong>Install / Repair Files</strong> after an interrupted update or missing-file error. The launcher only downloads files that need replacement.</p><a class="btn" href="/help">OPEN SUPPORT CENTER</a></section><section class="paperCard"><h3>Important</h3><p class="muted">Do not launch the game EXE directly. The launcher is the supported entry point and keeps your client current.</p></section></aside></div></main><%- include("partials/footer") %>
